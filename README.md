# Object-Detection-Raspberry

Object detection menggunakan tensorflow

git clone https://github.com/tensorflow/examples --depth 1

copy the git, run the setup and update the tflite support from 0.4.4 to 0.4.3
python -m pip install --upgrade tflite-support==0.4.3

